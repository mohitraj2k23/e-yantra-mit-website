# Backend

Backend logic goes here (e.g. Firebase config, Node/Express API for the contact form).

Suggested structure:
- `firebase-config.js` — Firebase initialization
- `api/` — API route handlers, if using Node/Express
- `.env.example` — sample environment variables (never commit real `.env`)
